version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run: 
          name: ant -lib lib/ -buildfile build.xml
          command: ant -lib lib/ -buildfile build.xml